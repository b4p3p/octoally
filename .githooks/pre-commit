#!/usr/bin/env bash
# Block commits that contain secrets/credentials. OctoAlly is a public repo.
# Installed via `git config core.hooksPath .githooks` (see the package.json
# "prepare" script, which runs on `npm install`).
exec "$(git rev-parse --show-toplevel)/scripts/secret-scan.sh" staged
